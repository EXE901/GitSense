import { SignupForm } from '@/components/auth/signup-form';

export const metadata = {
  title: 'Sign Up - GitSense',
  description: 'Create your GitSense account',
};

export default function SignUpPage() {
  return <SignupForm />;
}
